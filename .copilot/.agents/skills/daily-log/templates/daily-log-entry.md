## DD MMM YYYY

> One or two sentence summary of how the day went and what was the main focus.

### Repo Name

**Feature**
- Added `<ClassName/MethodName>` — brief description of what was introduced. (`path/to/file.cs`)

**Fix**
- Fixed `<issue>` in `<ClassName>` — brief description of the root cause and resolution. (`path/to/file.cs`)

**Refactor**
- Refactored `<ClassName>` — brief description of what changed and why. (`path/to/file.cs`)

**Chore**
- Updated `<dependency/config>` — brief description. (`path/to/file.json`)

**Docs**
- Updated `<document>` — brief description. (`path/to/file.md`)

**Other**
- Brief description. (`path/to/file`)

**In Progress** *(uncommitted)*
- Working on `<feature/fix>` — brief description of current state.

---
